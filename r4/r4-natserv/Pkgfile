# Description: R module natserv
# URL: https://cran.r-project.org/web/packages/natserv
# Maintainer: Petar Petrov, slackalaxy at gmail dot com
# Depends on: r r4-crul r4-jsonlite r4-tibble
# Optional: r4-knitr r4-testthat r4-vcr

name=r4-natserv
version=1.0.0
release=1
source=(https://cloud.r-project.org/src/contrib/natserv_${version}.tar.gz)

build() {
	cd natserv
	mkdir -p $PKG/usr/lib/R/library
	R CMD INSTALL . -l $PKG/usr/lib/R/library
}
