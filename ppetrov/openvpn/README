I use this only occasionally, from the command line, as follows:

1) Create these:

	mkdir -p /dev/net
	mknod /dev/net/tun c 10 200
	chmod 600 /dev/net/tun

2) start openvpn with a config:

	openvpn --config '/path/to/config/openvpn.*_TCP.ovpn'
