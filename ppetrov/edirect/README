This installs a number of scripts and several precompiled programs into:

	/usr/lib/edirect

To update the PATH environment variable:
	
	echo "export PATH=\/usr/lib/edirect:\$PATH" >> $HOME/.bash_profile

To automatically include the path in a script:

  export PATH=/usr/lib/edirect:${PATH}
