# Description: hostname file in /etc
# URL: /etc/hostname
# Maintainer: Petar Petrov, slackalaxy at gmail dot com
# Depends on:

name=hostname
version=00
release=1
source=($name.example)

build() {
	install -D -m 0644 $SRC/$name.example $PKG/etc/hostname
}
