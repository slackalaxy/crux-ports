# Description: hostname file in /etc
# URL: /etc/hostname
# Maintainer: Petar Petrov, slackalaxy at gmail dot com

name=etc-hostname
version=3.8
release=3
source=()

build() {
	mkdir -p $PKG/etc
	touch $PKG/etc/hostname
	cd $PKG/etc
	ln -s hostname HOSTNAME
}
